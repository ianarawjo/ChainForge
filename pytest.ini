# pytest.ini
[pytest]
pythonpath = .
markers =
    slow: loads a real ML model or hits the network; excluded by `-m "not slow"`
    needs_api_key: requires a provider API key in the environment
    needs_faiss: requires the optional faiss-cpu package
