// Stub for ESM-only provider SDKs that CRA's Jest (CommonJS) cannot transform.
// Tests here exercise ChainForge's own logic, not the vendor clients, so an
// empty module is sufficient to make the import graph resolvable.
export default {};
